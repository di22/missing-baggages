import { Routes } from '@angular/router';
import { MissingBaggagesComponent } from './pages/missing-baggages/missing-baggages.component';

export const routes: Routes = [
    {
      path: '',
      component: MissingBaggagesComponent,
    }
  ];